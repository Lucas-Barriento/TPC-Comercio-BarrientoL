CREATE DATABASE COMERCIO_DB
GO

USE COMERCIO_DB
GO
CREATE TABLE DBO.CLIENTES
	(ID INT PRIMARY KEY NOT NULL IDENTITY(1,1),
	NOMBRE VARCHAR(30) NOT NULL,
	APELLIDO VARCHAR(30) NOT NULL)

CREATE TABLE DBO.PROVEEDORES
	(ID INT PRIMARY KEY NOT NULL IDENTITY(1,1),
	NOMBRE VARCHAR(30) NOT NULL)

CREATE TABLE DBO.MARCAS
	(ID INT PRIMARY KEY NOT NULL IDENTITY(1,1),
	NOMBRE VARCHAR(30) NOT NULL)

CREATE TABLE DBO.CATEGORIAS
	(ID INT PRIMARY KEY NOT NULL IDENTITY(1,1),
	NOMBRE VARCHAR(30) NOT NULL)

CREATE TABLE DBO.PRODUCTOS
	(ID INT PRIMARY KEY NOT NULL,
	NOMBRE VARCHAR(50) NOT NULL,
	IDMARCA INT FOREIGN KEY REFERENCES MARCAS(ID),
	IDCATEGORIA INT FOREIGN KEY REFERENCES CATEGORIAS(ID),
	STOCK INT NOT NULL,
	STOCKMINIMO INT NOT NULL,
	GANANCIA DECIMAL(10,2) NOT NULL)

CREATE TABLE DBO.VENTAS
	(IDVENTA INT PRIMARY KEY IDENTITY(00000001,1) NOT NULL,
	IDCLIENTE INT FOREIGN KEY REFERENCES CLIENTES(ID),
	IDPRODUCTO INT FOREIGN KEY REFERENCES PRODUCTOS(ID),
	CANTIDAD INT NOT NULL,
	PRECIOUNITARIO MONEY NOT NULL,
	PRECIOPARCIAL MONEY NOT NULL,
	PRECIOFINAL MONEY NOT NULL)

CREATE TABLE DBO.COMPRAS
	(ID INT NOT NULL IDENTITY(00000001,1),
	IDPRODUCTO INT FOREIGN KEY REFERENCES PRODUCTOS(ID),
	IDPROVEEDOR INT FOREIGN KEY REFERENCES PROVEEDORES(ID),
	CANTIDAD INT NOT NULL,
	PRECIOUNITARIO MONEY NOT NULL)

	insert into MARCAS values ('Samsung'),('Topmega'),('Acer'),('Xiaomi'),('Nike')
	insert into CATEGORIAS values ('Smartphones'),('Relojes'),('Bicicletas'),('Indumentaria'),('Farmacia')

	insert into PROVEEDORES values ('Multipoint')

	
	